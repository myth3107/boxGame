function greet(name, lastName) { //name is the parameter
  console.log('Hello world ' + name + lastName);
}
greet('myth');// at the time of calling the function we have to pass the argument.

//calculating a value
function square(number) {
  return number * number;
}
console.log(square(2));//here we are calling the function and passing the argument. and also we are calling funtion 2 times.
